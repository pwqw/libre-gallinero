#!/data/data/com.termux/files/usr/bin/bash
# -*- coding: utf-8 -*-
# Shortcut para leer logs del ESP8266 (MODO NO INVASIVO)

set -e
set -u

cd "$HOME/libre-gallinero"

# Cargar funciones comunes
source "$HOME/libre-gallinero/termux/common.sh"

print_banner "LEER LOGS ESP8266 (PASIVO)"

# Ejecutar script de logs en modo no invasivo (por defecto)
# Para modo invasivo usar: python3 tools/read_logs.py heladera --restart
# Para ver historial usar: python3 tools/read_logs.py heladera --history
if python3 tools/read_logs.py heladera; then
    pause
else
    pause_error "Lectura de logs falló"
fi
